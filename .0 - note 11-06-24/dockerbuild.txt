	- elimina il container esistente da docker desktop

	da powershell:
	mvn clean package
	docker build --no-cache -t midoripol .
	docker run -d -p 8080:8080 --name midori-pol-container midoripol



	// per testare risorsa da terminale
	// prima occorre entrare nel folder, chiedi a chat gpt
	curl http://localhost:8080/midoripol/services/sendemail

	// altrimenti usare postman

	// comandi per creare un container

	mvn clean package
	docker build --no-cache -t midoripol .
	// con psw:
	docker run -d -p 8080:8080 --name midori-pol-container -e EMAIL_PASSWORD=urobyuigohnkzvsm midoripol
	// senza psw
	docker run -d -p 8080:8080 --name midori-pol-container midoripol




	GITHUB GESTIRE FILE GRANDI: 
	scarica https://git-lfs.github.com
	nella directory:
	git lfs install
	git lfs track "target/nome.war" 
	git add .gitattributes
	git rm --cached target/midoripol.war
	git add target/nome.war
	git commit -m "Track with Git LFS"
	git push origin master


	DOCKERHUB PSW: l43950g_la0582!
